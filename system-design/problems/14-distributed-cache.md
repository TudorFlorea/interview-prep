# Design Distributed Cache

[← Back to Problems](/system-design/problems/00-index.md)

---

## 🎯 Problem Statement

Design a distributed caching system like Redis or Memcached that provides low-latency key-value storage across multiple nodes with high availability.

**Difficulty**: 🔴 Hard (Tier 1)

---

## 1. Requirements Gathering

### Functional Requirements

1. **Get/Set/Delete** - Basic key-value operations
2. **TTL** - Automatic expiration
3. **Eviction** - Handle memory limits
4. **Replication** - Data redundancy
5. **Partitioning** - Distribute across nodes
6. **Atomic operations** - Increment, CAS (optional)

### Non-Functional Requirements

| Aspect | Requirement |
|--------|-------------|
| **Fault Tolerance** | 99.99% availability |
| **CAP** | AP with tunable consistency |
| **Scalability** | Millions of ops/second |
| **Latency** | &lt; 1ms P99 |
| **Durability** | Configurable (memory vs disk) |

---

## 2. Back of Envelope Calculations

```
Traffic:
- 1 million requests/second
- 80% reads, 20% writes
- 800K reads/sec, 200K writes/sec

Storage:
- 100 million keys
- Average key: 50 bytes
- Average value: 1 KB
- Total: 100M × 1 KB = 100 GB

Per Node (10 nodes):
- 10 GB per node
- 100K requests/sec per node
```

---

## 3. High-Level Architecture

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    DISTRIBUTED CACHE ARCHITECTURE                          │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│                            ┌─────────────┐                                 │
│                            │   Clients   │                                 │
│                            └──────┬──────┘                                 │
│                                   │                                        │
│                   ┌───────────────┴───────────────┐                       │
│                   │     Client Library             │                       │
│                   │  (Consistent Hashing Router)   │                       │
│                   └───────────────┬───────────────┘                       │
│                                   │                                        │
│       ┌───────────────────────────┼───────────────────────────┐           │
│       │                           │                           │            │
│       ▼                           ▼                           ▼            │
│  ┌─────────┐               ┌─────────┐               ┌─────────┐          │
│  │ Node 1  │               │ Node 2  │               │ Node N  │          │
│  │(Primary)│◄──replicate──►│(Primary)│◄──replicate──►│(Primary)│          │
│  └────┬────┘               └────┬────┘               └────┬────┘          │
│       │                         │                         │                │
│  ┌────┴────┐               ┌────┴────┐               ┌────┴────┐          │
│  │Replica 1│               │Replica 2│               │Replica N│          │
│  └─────────┘               └─────────┘               └─────────┘          │
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │                        COORDINATION                                   │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │ │
│  │  │   ZooKeeper  │  │   Gossip     │  │   Config     │              │ │
│  │  │  (Metadata)  │  │  (Heartbeat) │  │   Server     │              │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘              │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. Deep Dive: Consistent Hashing

```
┌─────────────────────────────────────────────────────────────────┐
│                   CONSISTENT HASHING                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Problem: How to distribute keys across nodes?                 │
│                                                                 │
│  Naive: hash(key) % N                                          │
│  Problem: Adding/removing node remaps most keys                │
│                                                                 │
│  Consistent Hashing:                                            │
│  ─────────────────────                                          │
│  • Map both keys and nodes to same hash ring                  │
│  • Key goes to first node clockwise on ring                   │
│  • Adding/removing node only affects neighbors                 │
│                                                                 │
│         ┌──────────────────────────────────────┐               │
│         │               Ring                    │               │
│         │                                       │               │
│         │           Node A                      │               │
│         │             ●                         │               │
│         │        ╱         ╲                    │               │
│         │   key1●           ●Node D            │               │
│         │       │           │                   │               │
│         │       │           │                   │               │
│         │   key2●           ●key3              │               │
│         │        ╲         ╱                    │               │
│         │          ●─────●                      │               │
│         │       Node B  Node C                 │               │
│         │                                       │               │
│         └──────────────────────────────────────┘               │
│                                                                 │
│  key1 → Node A (first clockwise)                               │
│  key2 → Node B                                                  │
│  key3 → Node D                                                  │
│                                                                 │
│  Virtual Nodes:                                                 │
│  ───────────────                                                │
│  Each physical node maps to multiple virtual nodes             │
│  Improves balance (avoids hotspots)                            │
│  Node A → A1, A2, A3, A4 (100-200 virtual nodes)              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Implementation

```python
import hashlib

class ConsistentHash:
    def __init__(self, nodes: List[str], virtual_nodes: int = 150):
        self.ring = {}  # hash -> node
        self.sorted_keys = []
        self.virtual_nodes = virtual_nodes
        
        for node in nodes:
            self.add_node(node)
    
    def add_node(self, node: str):
        for i in range(self.virtual_nodes):
            key = f"{node}:vn{i}"
            hash_val = self._hash(key)
            self.ring[hash_val] = node
            self.sorted_keys.append(hash_val)
        self.sorted_keys.sort()
    
    def remove_node(self, node: str):
        for i in range(self.virtual_nodes):
            key = f"{node}:vn{i}"
            hash_val = self._hash(key)
            del self.ring[hash_val]
            self.sorted_keys.remove(hash_val)
    
    def get_node(self, key: str) -> str:
        hash_val = self._hash(key)
        # Binary search for first node >= hash
        idx = bisect.bisect_left(self.sorted_keys, hash_val)
        if idx == len(self.sorted_keys):
            idx = 0  # Wrap around
        return self.ring[self.sorted_keys[idx]]
    
    def _hash(self, key: str) -> int:
        return int(hashlib.md5(key.encode()).hexdigest(), 16)
```

---

## 5. Deep Dive: Replication

```
┌─────────────────────────────────────────────────────────────────┐
│                   REPLICATION STRATEGIES                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Option 1: Primary-Replica                                     │
│  ──────────────────────────                                     │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐               │
│  │ Primary  │────►│ Replica 1│     │ Replica 2│               │
│  │  (R/W)   │     │   (R)    │     │   (R)    │               │
│  └──────────┘     └──────────┘     └──────────┘               │
│                                                                 │
│  • Writes go to primary                                        │
│  • Async replication to replicas                               │
│  • Reads can go to replicas (eventual consistency)            │
│                                                                 │
│  Option 2: Leaderless (Dynamo-style)                          │
│  ─────────────────────────────────                              │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐               │
│  │  Node 1  │◄───►│  Node 2  │◄───►│  Node 3  │               │
│  │  (R/W)   │     │  (R/W)   │     │  (R/W)   │               │
│  └──────────┘     └──────────┘     └──────────┘               │
│                                                                 │
│  • Write to W nodes (quorum)                                   │
│  • Read from R nodes (quorum)                                  │
│  • W + R > N ensures overlap                                   │
│                                                                 │
│  Quorum Example (N=3):                                          │
│  ─────────────────────                                          │
│  • W=2, R=2: Strong consistency (overlap = 1)                 │
│  • W=1, R=1: Eventual consistency (no overlap)                │
│  • W=3, R=1: Fast reads, slower writes                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 6. Deep Dive: Eviction Policies

```
┌─────────────────────────────────────────────────────────────────┐
│                   EVICTION POLICIES                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  When memory is full, which keys to remove?                    │
│                                                                 │
│  1. LRU (Least Recently Used)                                  │
│  ────────────────────────────                                   │
│  Remove keys that haven't been accessed recently               │
│  Implementation: Doubly linked list + HashMap                  │
│                                                                 │
│  ┌────────────────────────────────────────────────────┐        │
│  │  Head                                        Tail  │        │
│  │  (Most recent)                       (Least recent)│        │
│  │   ┌───┐   ┌───┐   ┌───┐   ┌───┐   ┌───┐         │        │
│  │   │ A │◄─►│ C │◄─►│ B │◄─►│ D │◄─►│ E │         │        │
│  │   └───┘   └───┘   └───┘   └───┘   └───┘         │        │
│  │                                                    │        │
│  │  On access: Move to head                          │        │
│  │  On evict: Remove from tail                       │        │
│  └────────────────────────────────────────────────────┘        │
│                                                                 │
│  2. LFU (Least Frequently Used)                                │
│  ──────────────────────────────                                 │
│  Track access count per key                                    │
│  Remove keys with lowest count                                 │
│  Redis: Approximation with counter decay                       │
│                                                                 │
│  3. TTL-based                                                   │
│  ─────────────                                                  │
│  Remove expired keys                                           │
│  Lazy expiration: Check on access                              │
│  Active expiration: Background sweep                           │
│                                                                 │
│  4. Random                                                      │
│  ──────────                                                     │
│  Simple, surprisingly effective                                │
│  Redis uses random sampling for LRU approximation              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### LRU Implementation

```python
class LRUCache:
    def __init__(self, capacity: int):
        self.capacity = capacity
        self.cache = {}  # key -> Node
        self.head = Node(None, None)  # Dummy head
        self.tail = Node(None, None)  # Dummy tail
        self.head.next = self.tail
        self.tail.prev = self.head
        
    def get(self, key: str) -> Optional[bytes]:
        if key not in self.cache:
            return None
        
        node = self.cache[key]
        self._move_to_head(node)
        return node.value
        
    def put(self, key: str, value: bytes):
        if key in self.cache:
            node = self.cache[key]
            node.value = value
            self._move_to_head(node)
        else:
            if len(self.cache) >= self.capacity:
                self._evict()
            
            node = Node(key, value)
            self.cache[key] = node
            self._add_to_head(node)
            
    def _evict(self):
        node = self.tail.prev
        self._remove(node)
        del self.cache[node.key]
        
    def _move_to_head(self, node):
        self._remove(node)
        self._add_to_head(node)
        
    def _add_to_head(self, node):
        node.next = self.head.next
        node.prev = self.head
        self.head.next.prev = node
        self.head.next = node
        
    def _remove(self, node):
        node.prev.next = node.next
        node.next.prev = node.prev
```

---

## 7. Cache Node Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                   CACHE NODE INTERNALS                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                     Cache Node                          │    │
│  │                                                          │    │
│  │  ┌────────────────────────────────────────────────────┐│    │
│  │  │              Network Layer (TCP)                    ││    │
│  │  │  • Non-blocking I/O (epoll/kqueue)                 ││    │
│  │  │  • Binary protocol (fast parsing)                  ││    │
│  │  └────────────────────────────────────────────────────┘│    │
│  │                         │                               │    │
│  │  ┌────────────────────────────────────────────────────┐│    │
│  │  │              Command Processor                      ││    │
│  │  │  • Single-threaded (Redis model)                   ││    │
│  │  │  • Event loop                                       ││    │
│  │  └────────────────────────────────────────────────────┘│    │
│  │                         │                               │    │
│  │  ┌────────────────────────────────────────────────────┐│    │
│  │  │              Data Structures                        ││    │
│  │  │  • Hash table (main store)                         ││    │
│  │  │  • Skip list (sorted sets)                         ││    │
│  │  │  • TTL heap (expiration tracking)                  ││    │
│  │  └────────────────────────────────────────────────────┘│    │
│  │                         │                               │    │
│  │  ┌────────────────────────────────────────────────────┐│    │
│  │  │              Memory Management                      ││    │
│  │  │  • Slab allocator (fixed size chunks)              ││    │
│  │  │  • Jemalloc (Redis)                                ││    │
│  │  └────────────────────────────────────────────────────┘│    │
│  │                         │                               │    │
│  │  ┌────────────────────────────────────────────────────┐│    │
│  │  │              Persistence (Optional)                 ││    │
│  │  │  • RDB (periodic snapshots)                        ││    │
│  │  │  • AOF (append-only file)                          ││    │
│  │  └────────────────────────────────────────────────────┘│    │
│  │                                                          │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                 │
│  Why Single-Threaded?                                           │
│  ─────────────────────                                          │
│  • No locks needed                                             │
│  • Cache operations are memory-bound, not CPU-bound            │
│  • Simpler, fewer bugs                                         │
│  • Multi-threading via sharding (multiple processes)          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 8. Failure Handling

```
┌─────────────────────────────────────────────────────────────────┐
│                   FAILURE HANDLING                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Node Failure Detection:                                        │
│  ─────────────────────────                                      │
│  • Heartbeat every 1 second                                    │
│  • Gossip protocol spreads failure info                        │
│  • After 3 missed heartbeats → Node marked down               │
│                                                                 │
│  Recovery Strategies:                                           │
│  ─────────────────────                                          │
│                                                                 │
│  1. Replica Promotion                                          │
│     ─────────────────────                                       │
│     Primary fails → Promote replica to primary                │
│     Automatic (with consensus) or manual                       │
│                                                                 │
│  2. Data Rehashing                                              │
│     ─────────────────                                           │
│     Keys on failed node → Rehash to remaining nodes          │
│     Only affects neighbors on hash ring                        │
│                                                                 │
│  3. Hinted Handoff                                              │
│     ────────────────────                                        │
│     Node temporarily unavailable:                              │
│     • Store writes on another node                            │
│     • "Hint" that data belongs elsewhere                      │
│     • When node returns, replay hints                         │
│                                                                 │
│  Client-Side Handling:                                          │
│  ───────────────────────                                        │
│  • Retry on different replica                                  │
│  • Circuit breaker for failing nodes                          │
│  • Local cache for read failures                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 9. API Design

```
# Basic operations
SET key value [EX seconds] [NX|XX]
GET key
DELETE key
MGET key1 key2 key3
MSET key1 val1 key2 val2

# Atomic operations
INCR key
DECR key
CAS key expected_value new_value  # Compare-and-swap

# TTL operations
EXPIRE key seconds
TTL key
PERSIST key

# Binary protocol (efficient)
┌────────┬────────┬───────────┬──────────┐
│ OpCode │ KeyLen │   Key     │  Value   │
│ 1 byte │ 2 bytes│  N bytes  │  M bytes │
└────────┴────────┴───────────┴──────────┘
```

---

## 10. Technology Choices

| Component | Technology | Rationale |
|-----------|------------|-----------|
| Core | Custom C/C++ | Performance |
| Hashing | MurmurHash | Fast, good distribution |
| Coordination | ZooKeeper/etcd | Consensus |
| Protocol | Redis RESP | Compatible |
| Memory | Jemalloc | Efficient allocation |

---

## 11. Key Takeaways

```
┌─────────────────────────────────────────────────────────────────┐
│                   KEY TAKEAWAYS                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. CONSISTENT HASHING                                          │
│     Minimal key redistribution on node changes                 │
│     Virtual nodes for better balance                           │
│                                                                 │
│  2. REPLICATION STRATEGIES                                      │
│     Primary-replica for simplicity                             │
│     Quorum-based for tunable consistency                      │
│                                                                 │
│  3. EVICTION POLICIES                                           │
│     LRU most common                                            │
│     Approximations work well in practice                       │
│                                                                 │
│  4. SINGLE-THREADED EVENT LOOP                                  │
│     Avoids locking overhead                                    │
│     Scale via sharding                                         │
│                                                                 │
│  5. GRACEFUL DEGRADATION                                        │
│     Handle node failures transparently                        │
│     Hinted handoff for temporary failures                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 12. References

- [12-consistent-hashing.md](/system-design/fundamentals/12-consistent-hashing.md) - Partitioning
- [07-caching.md](/system-design/fundamentals/07-caching.md) - Caching patterns
- [13-cap-theorem.md](/system-design/fundamentals/13-cap-theorem.md) - Consistency tradeoffs

---

[← Back to Problems](/system-design/problems/00-index.md) | [Next: Job Scheduler →](/system-design/problems/15-job-scheduler.md)
